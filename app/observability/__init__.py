"""Lightweight observability helpers (Week 6).

This package intentionally stays dependency-light: request IDs + structlog contextvars,
plus an in-memory metrics snapshot endpoint for local development.
"""

