services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: rag
      POSTGRES_PASSWORD: rag
      POSTGRES_DB: rag_notebook
    ports:
      - "5432:5432"
    volumes:
      - rag_notebook_pgdata:/var/lib/postgresql/data

volumes:
  rag_notebook_pgdata:
